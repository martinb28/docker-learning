name: CI Pipeline

on:
  push:
    branches: [main]

jobs:
  verificar:
    runs-on: ubuntu-latest
    steps:
      - name: Clonar repositorio
        uses: actions/checkout@v4

      - name: Verificar archivos
        run: |
          echo "Pipeline ejecutandose"
          ls -la